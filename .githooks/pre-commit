#!/bin/sh
# Refuses a commit made directly on a protected branch (see CLAUDE.md, git rules).
branch=$(git rev-parse --abbrev-ref HEAD)
exec bun "$(git rev-parse --show-toplevel)/tools/branch-guard.ts" "$branch"
