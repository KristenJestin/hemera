#!/bin/sh
# Refuses a commit made directly on a protected branch (see AGENTS.md, git rules).
branch=$(git rev-parse --abbrev-ref HEAD)
exec node "$(git rev-parse --show-toplevel)/tools/branch-guard.ts" "$branch"
